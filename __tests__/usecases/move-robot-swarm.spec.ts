import { describe, expect, it, beforeAll } from "@jest/globals";

describe("move all robots usecase", () => {
  it.todo(
    "should move all robots and return updated positions and orientations"
  );
});
